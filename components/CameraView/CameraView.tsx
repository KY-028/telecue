import { Camera, useCameraDevice, useCameraPermission, useMicrophonePermission } from 'react-native-vision-camera';

export { useCameraDevice, useCameraPermission, useMicrophonePermission };
export default Camera;
